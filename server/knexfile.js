// Update with your config settings.

/**
 * @type { Object.<string, import("knex").Knex.Config> }
 */
module.exports = {
  development: {
    client: "postgresql",
    connection: "postgres://postgres:docker@localhost/squad_deck",
  },

  staging: {
    client: "postgresql",
    connection: {
      database: "my_db",
      user: "username",
      password: "password",
    },
    pool: {
      min: 2,
      max: 10,
    },
    migrations: {
      tableName: "knex_migrations",
    },
  },

  production: {
    client: "postgresql",
    connection: {
      database: "my_db",
      user: "username",
      password: "password",
    },
    pool: {
      min: 2,
      max: 10,
    },
    migrations: {
      tableName: "knex_migrations",
    },
  },
};


// Copy/paste this string into development.connection for knex migrate/seed operations
"postgres://postgres:docker@localhost/squad_deck"

// Copy/paste this string into development.connection for making SQL queries from the Express server to the database
process.env.DB_CONNECTION_STRING
